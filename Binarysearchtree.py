__author__ = 'sanand'

class Node:
    def __init__(self, data):
        self.data = data
        self.left = none
        self.right = none
# insert a node in python
'''
    def insert(self, data):
    if self.data:
        if data < self.data:
            if self.data is None:
                self.left = Node(data)
            else:
                self.left.insert(data)
        elif data > self.data:
            if self.data is None:
                self.right = Node(data)
            else:
                self.right.insert(data)
    else:
        self.data = data
'''










